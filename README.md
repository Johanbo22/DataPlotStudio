# DataPlotStudio

DataPlotStudio is a GUI implementation of the Matplotlib and pandas data analytics and plotting capabilities. 
It is written using the PyQt6 GUI framework.

## Requirements
If you wish to build the application yourself, a requirements.txt is provided. To install with [pip](https://pip.pypa.io/en/stable/):

```
pip install requirements.txt
```